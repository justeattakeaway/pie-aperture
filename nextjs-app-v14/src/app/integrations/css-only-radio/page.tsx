import CssOnlyRadio from './css-only-radio';
import { type Metadata } from 'next';

export const metadata: Metadata = {
    title: 'CSS Only Radio',
}

export default function CssOnlyRadioPage() {
    return <CssOnlyRadio/>;
}
