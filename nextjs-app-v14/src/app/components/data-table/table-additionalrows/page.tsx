import TableAdditionalRows from './table-additionalrows';
import { type Metadata } from 'next';

export const metadata: Metadata = {
    title: 'DataTable - Additional Rows',
}

export default function TableAdditionalRowsPage() {
    return <TableAdditionalRows />;
}
