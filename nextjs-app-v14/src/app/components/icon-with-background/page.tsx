import IconWithBackground from './icon-with-background';
import { type Metadata } from 'next';

export const metadata: Metadata = { title: 'Icon With Background' }

export default function IconWithBackgroundPage() {
    return <IconWithBackground />;
}
