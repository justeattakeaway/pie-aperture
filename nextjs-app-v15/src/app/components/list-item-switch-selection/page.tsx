import ListItemSwitchSelection from './list-item-switch-selection';
import { type Metadata } from 'next';

export const metadata: Metadata = {
    title: 'List Item Switch Selection',
}

export default function ListItemSwitchSelectionPage() {
    return <ListItemSwitchSelection />;
}
