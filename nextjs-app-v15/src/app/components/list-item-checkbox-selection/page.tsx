import ListItemCheckboxSelection from './list-item-checkbox-selection';
import { type Metadata } from 'next';

export const metadata: Metadata = {
    title: 'List Item Checkbox Selection',
}

export default function ListItemCheckboxSelectionPage() {
    return <ListItemCheckboxSelection />;
}
