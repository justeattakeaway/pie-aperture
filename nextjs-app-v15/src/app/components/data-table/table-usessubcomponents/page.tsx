import TableUsesSubcomponents from './table-usessubcomponents';
import { type Metadata } from 'next';

export const metadata: Metadata = {
    title: 'DataTable - Uses Subcomponents',
}

export default function TableUsesSubcomponentsPage() {
    return <TableUsesSubcomponents />;
}
